# 胜算云 API 配置（必填）
# Shengsuanyun API Configuration (Required)
# 获取地址 / Get your API key at: https://router.shengsuanyun.com/
VITE_API_KEY=your_shengsuanyun_api_key_here

# 胜算云 API 基础地址（可选，默认值如下）
# Shengsuanyun API Base URL (Optional, default value shown below)
VITE_API_BASE_URL=https://router.shengsuanyun.com/api

# 通义千问备用后端地址（可选，默认 localhost:3001）
# Qwen backup backend URL (Optional, default: localhost:3001)
VITE_QWEN_BACKEND_URL=http://localhost:3001/api/qwen-mini

# 开发模式（可选）
# Development mode (Optional)
VITE_DEV_MODE=true
